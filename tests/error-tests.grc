import io

def teste() {
  i = 0;
  return 0;  
}

def teste2() : int {
  return true;  
}

def main() : int {
  var v : nt;
  var i : bool;
  var word = "abba" : string;
  var b = i, c : bool;
  var a = 2, b = -2 : int;
  var x : bool;
  var v[3] = {1,2,3} : int;

  if(v[i] == 3) {
    write "igual";  
  }
   
  if(a ? true : 0) {
    write "true";
  }else {
    write "false";
  }
  
  if(a) {}
  
  while(a) {
    var e : int;
    e = 5;
  }
  
  for(a = 0; 10; a++) {} 
   
  c = isWorking;
  read e;

  return;
}
